#include <stdio.h>
// tab ->
// shift + tab <-
// alt + shift + f 자동정렬
int main()
{
    // 10+20
    // 변수 num에 넣어서
    //출력

    int a = 10;
    int b = 20;
    int num = a + b;
    printf("num=%d", num);

    return 0;
}