#include <stdio.h>

/*
    변수 a에다가 3 넣고
    변수 b에다가 4 넣고
    합과 곱을 출력해보시오

    d:드라이브 이동
    cd work\c_work\221207
    dir 내용 보기
    shif home 뒤 end 앞 블록
*/
int main(){
    int a=3;
    int b=4;

    printf("%d + %d =%d\n",a,b,a+b);
    printf("%d * %d =%d",a,b,a*b);

 return 0;
}
